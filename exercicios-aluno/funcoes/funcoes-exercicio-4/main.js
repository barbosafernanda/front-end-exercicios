// exercicio 4

const mais = function (f, b) {
    let resultado = f + b;
    return resultado;
};

console.log (mais(3, 4));
console.log (mais(5, 6));

const menos = function (g, h) {
    let subtracao = g - h;
    return subtracao; 
};

console.log 

